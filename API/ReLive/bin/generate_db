#!/usr/bin/env perl
use strict;
use warnings;
use lib '../lib';
use lib 'lib';
use Anno::Schema;

my $schema = Anno::Schema->connect("dbi:sqlite:dbname=anno.db;host=localhost", '', '', {});
$schema->deploy({add_drop_table => 1});
